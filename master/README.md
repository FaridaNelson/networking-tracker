# Networking Tracker – Master Datasets

The `master/` directory contains the canonical networking datasets used for reporting, evidence generation, and summary statistics.

The files in this directory are **derived datasets**. They should not be edited manually. Each file is generated by a corresponding ETL script in the `src/` directory.

---

## Current Ledgers

### pending_connections_ledger.csv

Canonical ledger of LinkedIn pending connection invitations.

Generated by:

```
src/build_pending_connections_ledger.py
```

Primary key:

```
Pending Connection ID
(PEND-0001, PEND-0002, ...)
```

Source data:

- linkedin_pending_with_prentus_evidence.csv

---

### accepted_connections_ledger.csv

Canonical ledger of accepted LinkedIn connections.

Generated by:

```
src/build_accepted_connections_ledger.py
```

Primary key:

```
Accepted Connection ID
(CONN-0001, CONN-0002, ...)
```

Source data:

- linkedin_connections_filtered.csv
- prentus_contacts.csv

---

## Planned Ledgers

### conversations_ledger.csv

Canonical ledger of LinkedIn conversations.

Generated by:

```
src/build_conversations_ledger.py
```

Primary key:

```
Conversation Record ID
(CONV-0001, CONV-0002, ...)
```

Source data:

- linkedin_conversations_filtered.csv
- prentus_contacts.csv

---

## Planned Reports

### networking_summary_by_week.csv

Weekly networking activity summary generated from the canonical ledgers.

Generated by:

```
src/build_networking_summary_by_week.py
```

Primary key:

```
Reporting Week
(ISO format: YYYY-Www)
```

Week boundaries:

```
Monday through Sunday
```

Source data:

- pending_connections_ledger.csv
- accepted_connections_ledger.csv
- conversations_ledger.csv

Conversation message totals are assigned to the ISO week in which each
conversation started. They do not necessarily represent messages sent
during that calendar week.

---

### networking_summary_by_month.csv

Monthly networking activity summary generated from the canonical ledgers.

Generated by:

```
src/build_networking_summary_by_month.py
```

Primary key:

```
Reporting Month
(YYYY-MM)
```

Source data:

- pending_connections_ledger.csv
- accepted_connections_ledger.csv
- conversations_ledger.csv

Conversation message totals are assigned to the month in which each
conversation started. They do not necessarily represent messages sent
during that calendar month.

---

## ETL Workflow

```
Raw LinkedIn / Prentus Exports
            │
            ▼
      Parsing Scripts
            │
            ▼
     Processed Datasets
            │
            ▼
     Evidence Enrichment
            │
            ▼
     Canonical Ledgers
            │
            ▼
 Weekly / Monthly Reports
```

---

## Design Principles

- Raw exports are never modified.
- Processed datasets preserve the original records.
- Ledgers are canonical reporting datasets.
- Every ledger uses deterministic record IDs.
- Summary reports are always generated from the ledgers, never directly from raw exports.
